import { Dimensions } from 'react-native'

const measures = {
    "width": Dimensions.get('window').width,
    "height": Dimensions.get('window').height,
}

const colors = {
    "primary_bg": '#01EFE8'
}

export { measures, colors }